name: 'Changelog'
description: 'Check a pull request''s changelog fragment, seed a template for one, or roll fragments up into a release. Fragments are author-written; no bot ever writes one on the author''s behalf.'

inputs:
  mode:
    description: 'One of: check | seed | rollup'
    required: true
  changes-dir:
    description: 'Directory holding fragments.'
    required: false
    default: '.changes'
  changelog:
    description: 'Path to CHANGELOG.md (rollup mode).'
    required: false
    default: 'CHANGELOG.md'
  # check, seed
  pr:
    description: 'Pull request number.'
    required: false
  title:
    description: >-
      PR title. In check mode the change type is derived from it, which decides
      whether a fragment is required at all; in seed mode it seeds the type,
      falling back to chore.
    required: false
  # check
  changed-paths-file:
    description: >-
      File of `status<TAB>path` lines for the pull request's changed files. When
      given, a customer-visible change must add exactly one new fragment at
      exactly its own path.
    required: false
    default: ''
  changes-prefix:
    description: 'Repo-relative changes directory, for matching changed paths.'
    required: false
    default: '.changes'
  bot-author:
    description: >-
      Login of the PR author when it is a bot. Non-empty waives both the title
      convention and the fragment requirement.
    required: false
    default: ''
  # seed
  url:
    description: 'Pull request URL, recorded in the fragment.'
    required: false
  out:
    description: 'Write the seeded fragment here instead of <changes-dir>/preview/<pr>.json.'
    required: false
    default: ''
  # rollup
  version:
    description: 'Release version, x.y.z.'
    required: false
  date:
    description: 'Release date, ISO-8601.'
    required: false
  highlights:
    description: 'One-line highlights string for the release header.'
    required: false
    default: ''
  bump:
    description: >-
      Optional cross-check: patch | minor | major. The version decides the bump;
      a value that disagrees with it fails the release.
    required: false
    default: ''

outputs:
  rc:
    description: 'check mode: 0 pass, 1 author-fixable, 2 caller error.'
    value: ${{ steps.run.outputs.rc }}
  reason:
    description: >-
      check mode: ok | exempt-type | waived-bot | missing-fragment |
      invalid-fragment | pr-mismatch | type-mismatch | bad-title |
      stray-fragment | modified-fragment.
    value: ${{ steps.run.outputs.reason }}

runs:
  using: 'composite'
  steps:
    - name: Run changelog tool
      id: run
      shell: bash
      env:
        MODE: ${{ inputs.mode }}
        CHANGES_DIR: ${{ inputs.changes-dir }}
        CHANGELOG: ${{ inputs.changelog }}
        PR: ${{ inputs.pr }}
        TITLE: ${{ inputs.title }}
        BOT_AUTHOR: ${{ inputs.bot-author }}
        CHANGED_PATHS_FILE: ${{ inputs.changed-paths-file }}
        CHANGES_PREFIX: ${{ inputs.changes-prefix }}
        OUT: ${{ inputs.out }}
        URL: ${{ inputs.url }}
        VERSION: ${{ inputs.version }}
        DATE: ${{ inputs.date }}
        HIGHLIGHTS: ${{ inputs.highlights }}
        BUMP: ${{ inputs.bump }}
      run: |
        set -euo pipefail
        # TITLE and BOT_AUTHOR stay env vars throughout: interpolating a PR
        # title into a command line would be a shell-injection vector.
        SCRIPT="${GITHUB_ACTION_PATH}/scripts/changelog.py"
        case "${MODE}" in
          check)
            # Does not fail the step. The caller reads `rc` and `reason` and
            # decides -- a missing fragment earns a template comment first,
            # every other failure only gets the diagnostic above.
            OUT_FILE="$(mktemp)"
            EXTRA=()
            if [[ -n "${CHANGED_PATHS_FILE}" ]]; then
              EXTRA+=(--changed-paths-file "${CHANGED_PATHS_FILE}"
                      --changes-prefix "${CHANGES_PREFIX}")
            fi
            set +e
            python3 "${SCRIPT}" check --pr "${PR}" --title "${TITLE}" \
              --bot-author "${BOT_AUTHOR}" --changes-dir "${CHANGES_DIR}" \
              ${EXTRA[@]+"${EXTRA[@]}"} | tee "${OUT_FILE}"
            rc="${PIPESTATUS[0]}"
            set -e
            {
              echo "rc=${rc}"
              echo "reason=$(grep -o 'CHANGELOG_CHECK_REASON::[a-z-]*' "${OUT_FILE}" \
                              | tail -n1 | cut -d: -f3 || true)"
            } >> "${GITHUB_OUTPUT}"
            rm -f "${OUT_FILE}"
            ;;
          seed)
            EXTRA=()
            if [[ -n "${OUT}" ]]; then EXTRA+=(--out "${OUT}"); fi
            python3 "${SCRIPT}" seed --pr "${PR}" --title "${TITLE}" \
              --url "${URL}" --changes-dir "${CHANGES_DIR}" \
              ${EXTRA[@]+"${EXTRA[@]}"}
            ;;
          rollup)
            EXTRA=()
            if [[ -n "${BUMP}" ]]; then EXTRA+=(--bump "${BUMP}"); fi
            python3 "${SCRIPT}" rollup \
              --version "${VERSION}" --date "${DATE}" --highlights "${HIGHLIGHTS}" \
              --changes-dir "${CHANGES_DIR}" --changelog "${CHANGELOG}" \
              ${EXTRA[@]+"${EXTRA[@]}"}
            ;;
          *)
            echo "ERROR: unknown mode '${MODE}'" >&2; exit 2 ;;
        esac
