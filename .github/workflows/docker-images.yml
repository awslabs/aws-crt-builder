name: Docker Images

on: 
  push:
    paths:
      - '.github/docker-images/*/Dockerfile'
      - '.github/workflows/docker-images.yml'

jobs:
  manylinux1-x64:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout Sources
      uses: actions/checkout@v1
    
    - name: Build manylinux1-x64 image
      uses: whoan/docker-build-with-cache-action@v3
      with:
        username: "awslabs"
        password: "${{ secrets.GITHUB_TOKEN }}"
        image_name: awslabs/aws-crt-builder/manylinux1
        image_tag: x64
        registry: docker.pkg.github.com
        context: .github/docker-images/manylinux1-x64
        build_extra_args: "--compress=true"  

  manylinux1-x86:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout Sources
      uses: actions/checkout@v1
    
    - name: Build manylinux1-x86 image
      uses: whoan/docker-build-with-cache-action@v3
      with:
        username: "awslabs"
        password: "${{ secrets.GITHUB_TOKEN }}"
        image_name: awslabs/aws-crt-builder/manylinux1
        image_tag: x86
        registry: docker.pkg.github.com
        context: .github/docker-images/manylinux1-x86
        build_extra_args: "--compress=true"  
  
  Ubuntu-16-x64:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout Sources
      uses: actions/checkout@v1
    
    - name: Build Ubuntu-16-x64 image
      uses: whoan/docker-build-with-cache-action@v3
      with:
        username: "awslabs"
        password: "${{ secrets.GITHUB_TOKEN }}"
        image_name: awslabs/aws-crt-builder/ubuntu-16
        image_tag: x64
        registry: docker.pkg.github.com
        context: .github/docker-images/ubuntu-16-x64
        build_extra_args: "--compress=true"  

  AL2-x64:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout Sources
      uses: actions/checkout@v1
    
    - name: Build AL2-x64 image
      uses: whoan/docker-build-with-cache-action@v3
      with:
        username: "awslabs"
        password: "${{ secrets.GITHUB_TOKEN }}"
        image_name: awslabs/aws-crt-builder/al2
        image_tag: x64
        registry: docker.pkg.github.com
        context: .github/docker-images/al2-x64
        build_extra_args: "--compress=true"
